## 限流流程

- 把商品id和用户id拼上前缀
- 通过redisratelimiter调用限流的lua脚本，返回结果
- 传入key，还有当前时间戳(score)以及当前时间戳+uuid的请求id(member)
- 窗口左边界就是当前时间戳-窗口大小，右边界就是当前时间戳
- lua脚本会先删除窗口(zset)左边界之前的请求记录，然后判断窗口内的请求数量是否大于等于限制
- 如果小于限制就把当前请求记录添加到窗口内
- 失败返回0，成功返回1